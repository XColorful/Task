> 以下为refactor-impl (#19) 的提示词

```
这个项目是学习编程早期的作品，水平不高，基本就是命令行CRUD但是具有一定的模块化编程思想。
1. 这个脚本我至今仍在用，用于每天CRUD记录，包括但不限于记作业（最早期用途），记做事情的时间区间，自己约定一个字符串内的格式来记账（比如仍然是用一个string，但是人为规定用"; "分隔每一项，等未来写新脚本可以一键转换）
2. 刚开始编程的时候没技术，但便于等未来，比如现在技术成熟了，能利用一直记录的数据进行分析
3. 以上内容可作为后续任务的参考指引
现在需要完成以下内容：
1. （只读）调研其架构并写一个大致的架构文档，便于我快速了解早期的设计意图，写在根目录"架构.md"
2. 给出一个功能文档，说明其功能，写在根目录下"功能说明.md"
3. （只读）该脚本保存为pkl数据，也可以手动输入backup指令保存txt文件，你需要给出这个txt数据结构的说明文档，写在根目录"txt保存格式.md"
4. （只读）我现在拥有二十万行Java经验，现在开始Vibe coding，该项目需要用重构，原有的日常记录的用途仍保留，保留键盘输入执行CRUD操作，同时添加GUI按钮来CRUD的功能（比如经常记录的任务添加一个一键添加的按钮），并且要能支持图标分析，可能还会有3D的可视化（如按7个柱状为一行，按月分隔，选取某一项事件记录的时长直观可视化），考虑到以上目前待定的新增需求，确定重构方案的技术栈，写在根目录"新架构.md"
```

```
之前你写的“新架构”暂时搁置，我在Task根目录"docs/架构说明.md"新补充了需求说明，这个文件本身不要动，自行制定计划完成以下内容：
1. 确定使用Python还是改用Java，确定前后端技术栈，确定GUI解决方案，将分析及结果写在"docs/技术栈分析.md"
2. 顶层架构图，包含用户和软件之间的关系，用Mermaid绘制（基本要求：flowchart LR，最左侧以“用户 <==> 软件”开始），写在"docs/新架构图.md"（内部代码具体实现不属于顶层架构图），该文件内先写设计意图，再画markdown格式的mermaid
3. 在"docs/architecture/Home.md"写预期项目各目录的职责设计
4. 目前"docs/架构说明.md"是我的精简草稿，了解我的意图后在Task根目录"docs/"下完成一个全面的包含需求阐述、使用体验分析、运行环境、设计目标等各项内容的文档。注意任务1中的技术栈分析不需要重复复制该文档的内容，该文档也需要将详细的技术栈分析指向"docs/技术栈分析.md"

对于完成以上任务，可以参考以下优秀资源：
- 在BattleRoyale目录下docs/architecture/Home.md有任务3的优秀示范
- 可以参考BattleRoyale目录下docs/architecture/design里同构分形和线性熵增的设计思想
- 可以参考BattleRoyale目录下docs/architecture/common/game/game-framework.md的设计
```

```
我澄清以下内容：
- 记录类型需要可扩展，之后添加的多种统计方式，以及新的记录类型，视为模组化（但是不专门重新开项目），从而不使用record枚举预先定义，需数据存字符串+启动时注册+运行时hash get转接对应的处理模块，从而避免在core/model里与扩展模块耦合
- Json存储需要进一步，由于一个Tasker会持续堆积，从而软件配置目录指定存储目录，一个Tasker在内部再单独使用一个文件夹，文件夹内所有json文件构成完整tasker，以进一步减少OneDrive同步开销（分段存储），新的设计需要考虑到这一点
- core模块应只有抽象父类的逻辑，default、timer、account的处理都属于扩展模块（这样内置的也算扩展模块），方便统一后续增量更新的开发流
- 我拥有大量Java经验，仅仅是我能够阅读项目代码，而不代表不接受Python技术栈；我的Java经验仅能提供高质量评价，但产出效率远不及Agent自动化，因而我的产出效率不作为选取的依据
- 我看到模块依赖关系里有命令系统，这个是可选的，新的前端界面同时支持键盘输入跳转以及鼠标点击按钮跳转，添加task的时候也同时支持旧的输入内容+回车的方式，也支持预设task模板一键点击添加的方式，这个系统不应该做成是纯键盘的command系统
- 对于启动时的性能分析，由于常驻后台，只需要支持快捷键打开即可，如果常驻后台需要占用的内存少，JVM启动时的开销可以忽略
- 对于画图技术栈，大多数时间只需要记录，需要看图/画图的时候运用微服务的思想，按需加载，允许画图时专门画额外开销进行启动，可以是单独的进程或线程（避免连续看表时反复启动）
	- 由于允许画图时额外开销，可以是浏览器画图的方案，只要关闭画图时能释放资源，可以是能单独启动的进程（只要能跟常驻的进程通信）
- 对于依赖项，Python只要能通过requirement.txt安装就视为完全接受（无论库体积大小），Java方案只要能自动下载.jar也行
- 对于画图，目前的旧项目只有python简单画图，之后可能需要自定义的画图，会引入3D立体图（比如将计时器的内容按照每一行7格7天，每到月与月的分割周就强制换行，从而形成一条路的柱状图可视化），需要提前考虑这个技术栈，如果Java引入3D比Python不方便，这也是个减分项
- 对于Json存储，从磁盘读取的逻辑可以写在core里，让扩展模块完成解析json的部分，同时需要避免启动时就读取外全部json，应该额外存一个index来使得在不进入某个tasker的时候也能提供一个大概的信息
- 如果Python能把Java里接口的写法转成对应的Python设计，我的重构经验（架构评析能力）就也能服务于Python
- 由于数据分析功能是独立的，可以做在主界面，而不是进入tasker后的界面，并且启动数据统计功能后应该占用主窗口，直到关闭图标查看并释放资源后再回到正常的软件操作

基于以上内容，自行制定计划来完成四个文档重写。并且你重新考虑按照什么顺序来写，避免出现尚未思考完必要内容而提前编写导致产生的幻觉。
```

```
允许3D画图的时候启动浏览器而不是内置在软件窗口里。
对于启动浏览器而不是内置浏览器来查看，软件主窗口只需要一个返回键即可，期间应该让浏览器能跟软件交互数据，或者一开始构建网页的时候就做好（应该需要支持交互数据，因为不会一下子显示多年的可视化）
如果这样更方便或者有其他好处的话，请基于此重新检查并修改文档相关部分
```

```
现在我在"docs/_界面设计.md"给出前端描述的草稿。
- 阅读我的设计后写在"docs/界面设计.md"
- 这算做是进一步清晰化需求，之前四个文档可以相应地改动
- 如果目前的说明还有不清晰或缺失的地方请说明，我在了解之后再决定是否要开始根据已有文档，对architecture/Home.md里提到的文件都在architecture下面补充详细的规格文档
```

```
1.Tasker入口方式：
- 主界面修改，在背景半透明显示github链接和作者等内容，从而内容显示区留给tasker列表，点击进去，按照taskers.json里读取的顺序排序。注意所有地方json写入和读取都需要保持顺序
2. 快捷按钮放在右侧，本来会显示所有可用的指令，所有显示的指令都可以点击，点击后即输入。快捷指令是写在taskers.json里绑定在tasker上的属性，可增加或删除，指令名自定义，创建指令的模板数据存在taskers.json里，怎么用这个模板来创建就是代码实现的功能（需要处理自动填补日期以及是否使用默认值的判定）
3. 进入图标模式后，处理好图标需要的数据就不影响输入框，遇到动态读取数据的未来待添加的3D图，读取数据时实时向主进程拿，注意并发安全即可
4. 右侧搜索结果仍然用表格样式
5. account是旧软件的，wallet是我在旧软件用default tasker里的task格式，跟account无关，正常来说完成了default就能读取数据，只是特殊解析（从default转换成待添加的wallet类型）待完成；account仍然复刻旧软件的实现

另外，之前提到的"+"开头直接视为"search "，逻辑改成先判断输入是否以这个特殊字符开头，如果是就替换为"search "，然后再正常执行后续的空格分隔解析逻辑，从而复用同一个search功能。

如果还有需要待澄清的，继续提出。
```

```
技术栈分析里有误：
- tasker文件夹下不需要index.json，直接按照文件名足以选组最近的两项
- 已经确定根目录下用taskers.json，而不要混用tasker_list.json这个名称，其他文档如有误用的需要修改
- 不用额外维护一个python转换并不是显著优势，这算一个普通优势（因为是一次性），但是python相较于Java写得更快更容易Agent自动化，但是弱类型（编译期不会因为类型不对而报错）

增加以下说明，相应文档需要更新说明：
- 之后写的代码放在根目录/src/下，比如文档里的core目录实际会在/src/core里，这样就暂时不影响旧软件的代码，方便读取而不会混用
- tasker文件夹下内容按task数量分割，而不要按时间分割，默认每个segment存500个task
- "+"的处理视作指令的预处理，把该流程清楚地呈现出来，从而后续需要添加别的预处理时方便
- 主界面的快捷指令是列出所有tasker的快捷指令，点击后自动进入对应tasker并执行，tasker类在设计的时候就应该支持在主界面的点击时能触发，或者是直接调用原始的通知给tasker（而不是只有在tasker里的按钮才触发信号）
- backup写入时不是直接复制，而是全部tasker立即读取并写入，而不是绕过读取直接复制文件
- reload选取可备份的内容的时候，直接以文件夹下是否有taskers.json为准，直到整个备份读取完之前都不进行替换及写入
- 界面设计里的“图片查看”暂不写进文档（目前没有计划）
- tasker内部存储task的结构需要注意，保留读取时用了哪些json文件，写回时注意哪些段是脏数据需要写入，在写入前刚好卡在扩容和缩容边缘时，应该都不影响最终写入时的判定
```

```
现在对architecture/Home.md里提到的文件都在architecture下相应目录都补充详细的规格文档"SPEC.md"，这应该会在每个目录下都有一份，以便于编写代码时TODO和SPEC结合起来
- 文档需要清晰描述接口职责，参数等信息，使后续基于该文档实现代码时不引发歧义
- 对于参数需要显示说明是哪个类，各文档之间需保持一致，以避免引发歧义
- 除了相应的规格文档，还需要在根目录完成"TODO.md"，并在文档目标的代码目录处补充想用的TODO.md，表示需要从哪个architecture文档获取信息（不局限于当前目录下的文件），还剩下什么没有完成
- 根目录的TODO.md不仅要列出全项目各处哪些地方需要完成代码，也需要制定阶段性测试，从而决定代码编写的顺序，不一定一次性完成某个TODO.md，只需要达到测试阶段就可以，完成测试后再继续后续编写
- skills为本地文件读取形式，见CLAUDE.md按需读取skills目录下内容
```

```
现在完成TODO阶段0，自行完成验证测试
```

```
继续完成TODO阶段1，完成后commit内容为"(agent): TODO phase 1 impl"，description不需要写。不要push到远程
```

```
你不要修改本地commit的账号，我已经重置了。并且你之前commit没有包含全部文件，我已经回退了，你现在重新检查测试是否通过，并且把变动的文件都提交到commit里，不要新增local账号。
```

```
我已经提交好了，接着完成Phase2
```

```
继续完成Phase3
```

```
继续完成Phase4
```

```
继续完成Phase5
```

```
我没跑通Phase5的测试，进入了没有DISPLAY的分支并立即退出。并且之前是没有PySide6环境的，我已经手动pip install安装。你看是否这个测试无法进行？我暂时把commit回退到未提交的状态。
```

```
我运行测试如下，你看看中间提到的问题？
Test 1 OK: StyledButton
Test 2 OK: ToastNotification
Test 3 OK: ConfirmDialog
Test 4 OK: MainWindow
Test 5 OK: SystemTray
Test 6 OK: MainController
QFontDatabase: Cannot find font directory C:/Users/XColo/AppData/Local/Programs/Python/Python314/Lib/site-packages/PySide6/lib/fonts.
Note that Qt no longer ships fonts. Deploy some (from https://dejavu-fonts.github.io/ for example) or switch to fontconfig.
Test 7 OK: MainWindow layout built
Test 8 OK: SystemTray created
Test 9 OK: MainController initialized
Test 10 OK: InputPreprocessor
Test 11 OK: log output
Test 12 OK: commands panel
Test 13 OK: input dispatch

ALL 13 TESTS PASSED
PS D:\Github\Task> 
```

```
继续完成Phase6
```

```
继续完成Phase7，提交的时候user.email="XColor_ful@outlook.com" -c user.name="XiaoColorful"
```

```
继续完成Phase8，提交的时候user.email="XColor_ful@outlook.com" -c user.name="XiaoColorful"
```
