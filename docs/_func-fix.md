> 以下为func-fix (#21) 的提示词

```
1. 从主界面输入内容选中tasker的时候，输入的英文需要是大小写敏感的，不要自动转换
2.tasker里的index仍然没有按照-1, -2, -3……这样的顺修排，请研究是否是这个框架固有的问题，否则请修复
3.json写入的时候，缩进使用\t而不要用空格
```

```
你不准更改当前分支，commit前需由我确认
1. 从主界面输入内容选中tasker的时候，输入的英文需要是大小写敏感的，不要自动转换
2.tasker里的index仍然没有按照-1, -2, -3……这样的顺修排，请研究是否是这个框架固有的问题，否则请修复
3.json写入的时候，缩进使用\t而不要用空格
```

```
你不准执行git操作，只允许浏览代码并修改，不得执行commit，完成以下内容后由我审阅：
1. 从主界面输入内容选中tasker的时候，输入的英文需要是大小写敏感的，不要自动转换
```

```
你不准执行git操作，只允许浏览代码并修改，不得执行commit，完成以下内容后由我审阅：
1.json写入的时候，缩进使用\t而不要用空格
```

```
你不准执行git操作，只允许浏览代码并修改，不得执行commit，完成以下内容后由我审阅：
1.tasker里的index仍然没有按照-1, -2, -3……这样的顺修排，请研究是否是这个框架固有的问题并用聊天的方式回复，否则请修复
执行完后不要反复循环执行command，完成工作后就立即报告并停止
```