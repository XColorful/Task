# Task 项目重构方案 & 新架构设计

> 本文档基于原始项目分析、新增需求（GUI、图表分析、3D可视化）以及开发者的 Java 技术背景，给出重构的技术栈选型和架构设计。

## 一、需求回顾

### 1.1 保留功能
- 日常 CRUD 记录（Tasker → Task 的核心数据模型）
- 键盘命令行输入执行 CRUD 操作（CLI 交互方式保留）
- content 字段内 `"; "` 分隔的约定格式
- txt 备份导出（向后兼容旧数据）
- pkl 数据读取（迁移路径）

### 1.2 新增需求
- **GUI 界面**：按钮式 CRUD 操作（如一键添加常用记录）
- **图表分析**：按属性/日期统计、饼图、柱状图、趋势图
- **3D 可视化**：以 7 个柱状为一行的按月网格布局，展示事件时长
- **键盘快捷键**：在 GUI 中保留键盘操作能力

### 1.3 非功能性需求
- 开发者拥有约 20 万行 Java 经验
- 采用 Vibe Coding 方式开发
- 项目为个人日常工具，单用户运行

## 二、技术栈推荐

### 2.1 主推方案：FastAPI + React + ECharts

```
┌──────────────────────────────────────────────┐
│                  Browser                      │
│  ┌────────────┐  ┌──────────┐  ┌──────────┐ │
│  │ React UI   │  │ ECharts  │  │Three.js  │ │
│  │(TypeScript)│  │ (2D图表) │  │(3D可视化)│ │
│  └─────┬──────┘  └──────────┘  └──────────┘ │
└────────┼─────────────────────────────────────┘
         │ HTTP / WebSocket
┌────────┼─────────────────────────────────────┐
│        ▼           FastAPI (Python)           │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ REST API │  │ 分析引擎 │  │ 数据迁移   │ │
│  │ CRUD端点 │  │pandas/np │  │ pkl→SQLite │ │
│  └────┬─────┘  └──────────┘  └────────────┘ │
│       │                                      │
│  ┌────▼─────┐                                │
│  │ SQLite   │  ← 替代 pkl，结构化存储        │
│  └──────────┘                                │
└──────────────────────────────────────────────┘
```

**选型理由：**

| 组件 | 选择 | 理由 |
|------|------|------|
| **后端框架** | FastAPI | 现代 Python Web 框架，异步支持、自动 OpenAPI 文档、类型安全（Pydantic） |
| **数据库** | SQLite | 单文件零配置、无需服务进程、足够个人使用、可直接查询分析 |
| **ORM** | SQLAlchemy 2.0 | Python 生态最成熟的 ORM，与 FastAPI 配合良好 |
| **前端框架** | React 18 + TypeScript | 类型系统接近 Java 心智模型；生态最丰富；适合复杂交互 |
| **2D 图表** | ECharts (echarts-for-react) | 功能全面（柱状图、饼图、折线图、热力图）、中文友好、配置式 API |
| **3D 可视化** | ECharts GL + Three.js | ECharts GL 处理 3D 柱状图（按月网格）；Three.js 处理更复杂场景 |
| **样式** | Tailwind CSS | 原子化 CSS，Vibe Coding 效率高 |
| **数据迁移** | 自定义 Python 脚本 | 读取原 pkl → 写入 SQLite（一次性） |

**为什么不是 Electron？**
Electron 打包体积大（~200MB），对于个人单机工具过于笨重。浏览器访问 `localhost:5173` 即可使用，也可用 PWA 或 pywebview 封装为桌面窗口。

### 2.2 备选方案：纯 Python 桌面（PySide6）

如果坚持纯 Python 桌面应用：

| 组件 | 选择 |
|------|------|
| GUI 框架 | PySide6（Qt for Python） |
| 2D 图表 | matplotlib（嵌入 Qt） |
| 3D 可视化 | PyOpenGL / Qt 3D |
| 数据库 | SQLite（与主方案相同） |

**不推荐的原因：** Qt 的 3D 可视化能力有限，matplotlib 交互性不如 ECharts，UI 开发效率低于 React。

## 三、新数据模型设计

### 3.1 核心表结构（SQLite）

```sql
-- Tasker（容器）
CREATE TABLE tasker (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    label TEXT NOT NULL,
    description TEXT DEFAULT '',
    type TEXT NOT NULL DEFAULT 'default',  -- 'default' | 'timer' | 'account' | 'label'
    version TEXT DEFAULT '1.0',
    create_date TEXT NOT NULL,             -- YYYY_MM_DD
    sort_order INTEGER DEFAULT 0,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now'))
);

-- Task（记录项）
CREATE TABLE task (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tasker_id INTEGER NOT NULL REFERENCES tasker(id) ON DELETE CASCADE,
    type TEXT NOT NULL DEFAULT 'default',
    version TEXT DEFAULT '1.0',
    date TEXT NOT NULL,                    -- 事件日期 YYYY_MM_DD
    attribute TEXT DEFAULT '',             -- 属性分类
    content TEXT NOT NULL DEFAULT '',      -- 内容
    comment TEXT DEFAULT '',               -- 注释
    create_date TEXT NOT NULL,             -- 记录创建日期
    -- timer 专用字段
    start_time TEXT,                       -- YYYY_MM_DD-HH:MM
    end_time TEXT,                         -- YYYY_MM_DD-HH:MM
    duration_minutes INTEGER,             -- 计算得出的持续分钟数
    -- account 专用字段
    account_type TEXT,                     -- 账号平台
    account_label TEXT,                    -- 账号标签
    account_password TEXT,                 -- 密码（应加密存储）
    sort_order INTEGER DEFAULT 0,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now'))
);

-- Task 标签（替代 label 模块的 task.dict）
CREATE TABLE task_label (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER NOT NULL REFERENCES task(id) ON DELETE CASCADE,
    key TEXT NOT NULL,
    value TEXT NOT NULL
);

-- Account 补充信息
CREATE TABLE account_info (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER NOT NULL REFERENCES task(id) ON DELETE CASCADE,
    info_key TEXT NOT NULL,               -- 'description', 'login_name', 'verified_phone' 等
    info_value TEXT NOT NULL,
    sort_order INTEGER DEFAULT 0
);

-- 快捷按钮（一键添加常用记录）
CREATE TABLE quick_button (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tasker_id INTEGER NOT NULL REFERENCES tasker(id) ON DELETE CASCADE,
    label TEXT NOT NULL,                   -- 按钮显示文本
    preset_date TEXT,                      -- 预设日期（空=当天）
    preset_attribute TEXT,                 -- 预设属性
    preset_content TEXT,                   -- 预设内容
    preset_comment TEXT,                   -- 预设注释
    icon TEXT,                             -- 图标名（Lucide icon name）
    color TEXT DEFAULT '#6366f1',          -- 按钮颜色
    sort_order INTEGER DEFAULT 0,
    usage_count INTEGER DEFAULT 0          -- 使用次数统计
);
```

### 3.2 设计要点

- **单表多用：** Task 表通过 `type` 字段区分 Default/Timer/Account/Label 类型，timer 和 account 专用字段可为 NULL
- **分析友好：** `duration_minutes` 冗余存储便于直接聚合查询；`date` 字段可用于按月分组
- **快捷按钮：** `quick_button` 表存储用户的常用记录模板，一键即可创建 Task
- **密码安全：** Account 密码应使用 Python `cryptography` 库加密后再存储

## 四、API 设计概览

```
GET    /api/taskers              → 列出所有 Tasker
POST   /api/taskers              → 创建 Tasker
PUT    /api/taskers/{id}         → 更新 Tasker
DELETE /api/taskers/{id}         → 删除 Tasker
PUT    /api/taskers/sort         → 排列 Tasker

GET    /api/taskers/{id}/tasks   → 列出 Tasker 下的 Task
POST   /api/taskers/{id}/tasks   → 创建 Task（含快捷按钮调用）
PUT    /api/tasks/{id}            → 更新 Task
DELETE /api/tasks/{id}            → 删除 Task

GET    /api/tasks/search?q=xxx   → 全文搜索 Task
POST   /api/tasks/batch          → 批量创建（如导入旧数据）

GET    /api/quick-buttons        → 列出快捷按钮
POST   /api/quick-buttons        → 创建快捷按钮
PUT    /api/quick-buttons/{id}   → 更新快捷按钮
DELETE /api/quick-buttons/{id}   → 删除快捷按钮
POST   /api/quick-buttons/{id}/trigger → 触发快捷按钮（创建Task，usage_count+1）

GET    /api/analysis/attributes  → 按属性统计（饼图/柱状图数据）
GET    /api/analysis/monthly     → 按月统计（趋势图数据）
GET    /api/analysis/duration    → 时长分析（按属性/按月）
GET    /api/analysis/heatmap     → 热力图数据（日期×属性）

GET    /api/visualization/3d-monthly → 3D 柱状图数据（按月网格）
GET    /api/visualization/calendar   → 日历热力图数据
```

## 五、前端架构

### 5.1 路由结构

```
/                        → 主控制台（仪表盘）
/taskers                 → Tasker 列表管理
/taskers/:id             → Tasker 详情（Task 列表 + CRUD）
/taskers/:id/tasks/:tid  → Task 详情/编辑
/analysis                → 图表分析中心（2D 图表）
/visualization           → 3D 可视化页面
/settings                → 设置页（快捷按钮管理、配置）
/migration               → 数据迁移工具（pkl → SQLite）
```

### 5.2 关键组件

```
App
├── Sidebar (导航)
├── MainLayout
│   ├── Dashboard (仪表盘：今日概览、最近记录)
│   ├── TaskerView
│   │   ├── TaskerList (左侧列表)
│   │   ├── TaskTable (中间表格，可键盘导航)
│   │   └── QuickButtonBar (右侧/顶部快捷按钮)
│   ├── TaskEditModal (创建/编辑弹窗)
│   ├── AnalysisPanel
│   │   ├── AttributePieChart (属性饼图)
│   │   ├── MonthlyBarChart (月度柱状图)
│   │   └── DurationChart (时长趋势图)
│   └── Visualization3D
│       ├── MonthlyGrid3D (3D月度网格柱状图)
│       └── CalendarHeatmap (日历热力图)
```

### 5.3 键盘交互设计

- `Ctrl+K`：全局命令面板（类似 VS Code）
- `/`：聚焦搜索框
- `Ctrl+N`：快速新建 Task
- `Ctrl+E`：编辑选中 Task
- `Delete`：删除选中 Task
- `1-9`：触发对应序号的快捷按钮
- `↑↓`：在 Task 列表中导航
- `Esc`：关闭弹窗/返回上级

### 5.4 快捷按钮设计

在 Tasker 详情页的右侧或顶部，显示一排快捷按钮：

```
┌─────────────────────────────────────────────────┐
│  [📝 记作业] [⏰ 开始编程] [🍜 吃饭] [🏃 运动] │
│  [💡 灵感] [+ 添加按钮]                         │
└─────────────────────────────────────────────────┘
```

- 每个按钮可配置：标签、图标、预设属性、预设内容、颜色
- 点击即创建一条 Task，自动填充预设字段
- 按钮按 `usage_count` 排序，常用的自动靠前
- 按数字键 `1-9` 可键盘触发

## 六、3D 可视化方案

### 6.1 "7柱状一行，按月分隔" 实现

使用 ECharts GL 的 3D 柱状图（bar3D），数据组织方式：

```
X轴：每月内的第N个事件（0-6为一行，7-13为下一行...）
Z轴：月份（2023_09, 2023_10, 2023_11...）
Y轴（高度）：持续时长（分钟）
颜色：按属性分类着色
```

伪代码：
```javascript
// 将数据组织为 7列×N行 的网格
const GRID_COLS = 7;
const data = tasks.map((task, index) => ({
    x: index % GRID_COLS,           // 列 (0-6)
    z: Math.floor(index / GRID_COLS), // 行
    y: task.duration_minutes,        // 高度 = 时长
    // 按月份偏移 Z 轴
    monthIndex: ...
}));
```

### 6.2 备选：Three.js 实现

如 ECharts GL 不满足定制需求，可使用 Three.js：
- 每个 Task 渲染为一个 BoxGeometry
- 高度 = 时长，颜色 = 属性分类
- 按月排列在网格中
- OrbitControls 实现旋转/缩放
- CSS2DRenderer 显示标签

## 七、数据迁移策略

### 7.1 迁移脚本

```python
# migration.py —— 从 pkl 迁移到 SQLite

import pickle
from new_app.database import SessionLocal
from new_app.models import Tasker, Task, QuickButton

def migrate(pkl_path: str):
    with open(pkl_path, 'rb') as f:
        old_tasker_list = pickle.load(f)
    
    db = SessionLocal()
    for old_tasker in old_tasker_list:
        new_tasker = Tasker(
            label=old_tasker.tasker_label,
            description=old_tasker.description,
            type=old_tasker.type,
            version=old_tasker.version,
            create_date=old_tasker.create_date
        )
        db.add(new_tasker)
        db.flush()
        
        for old_task in old_tasker.task_list:
            new_task = Task(
                tasker_id=new_tasker.id,
                type=old_task.type,
                date=old_task.date,
                attribute=old_task.attribute,
                content=old_task.content,
                comment=old_task.comment,
                create_date=old_task.create_date,
                # timer 字段
                start_time=getattr(old_task, 'start_time', None),
                end_time=getattr(old_task, 'end_time', None),
                # account 字段
                account_type=getattr(old_task, 'account_type', None),
                ...
            )
            db.add(new_task)
    
    db.commit()
```

### 7.2 迁移步骤

1. 用户在前端迁移页面选择 pkl 文件
2. 后端读取 pkl，解析所有 Tasker 和 Task
3. 写入 SQLite 数据库
4. 显示迁移报告（Tasker 数、Task 数、跳过/错误项）
5. 原始 pkl 文件保留不删除

## 八、项目结构

```
new-task/
├── backend/
│   ├── main.py                  # FastAPI 入口
│   ├── config.py                # 配置
│   ├── database.py              # SQLite 连接
│   ├── models/                  # SQLAlchemy 模型
│   │   ├── __init__.py
│   │   ├── tasker.py
│   │   ├── task.py
│   │   └── quick_button.py
│   ├── schemas/                 # Pydantic 序列化模型
│   ├── routers/                 # API 路由
│   │   ├── taskers.py
│   │   ├── tasks.py
│   │   ├── analysis.py
│   │   └── quick_buttons.py
│   ├── services/                # 业务逻辑
│   │   ├── crud.py
│   │   ├── analysis.py
│   │   └── migration.py
│   └── tests/
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── pages/
│   │   ├── components/
│   │   ├── hooks/
│   │   └── api/                 # API 调用封装
│   ├── package.json
│   └── tailwind.config.js
├── scripts/
│   └── migrate.py               # 独立数据迁移脚本
└── requirements.txt
```

## 九、开发路线图

| 阶段 | 内容 | 预计 |
|------|------|------|
| **Phase 1** | 搭建 FastAPI + React 骨架，实现 SQLite 数据模型 | 基础架构 |
| **Phase 2** | 实现 Tasker/Task 的 CRUD API + 前端界面 | 核心 CRUD |
| **Phase 3** | 键盘快捷键 + 快捷按钮功能 | 效率提升 |
| **Phase 4** | 数据迁移工具（pkl → SQLite） | 旧数据导入 |
| **Phase 5** | 2D 图表分析（ECharts：饼图、柱状图、趋势图） | 可视化基础 |
| **Phase 6** | 3D 可视化（ECharts GL / Three.js） | 高级可视化 |
| **Phase 7** | 文本搜索、高级筛选、导入导出 | 增强功能 |

## 十、备选简化方案

如果全栈方案前期负担过重，可先做**最小可行版本（MVP）**：

- 仅 FastAPI + 简单 HTML 模板（Jinja2 + HTMX + ECharts CDN）
- 无需 Node.js 构建工具链
- 功能齐全但 UI 较朴素
- 后续可逐步迁移到 React

此方案可将 Phase 1-4 的时间压缩到数天内完成，快速验证后再决定是否投入 React 前端开发。
