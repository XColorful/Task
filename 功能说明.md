# Task 功能说明文档

> 本文档说明 Task 项目的所有用户可见功能，按模块组织。

## 一、程序启动

运行 `main.py` 启动程序。启动后显示欢迎信息（GitHub 仓库地址、版本号、贡献者）。

**启动流程：**
1. 读取 `settings.txt` 配置
2. 加载 `default_pkg`（必须）和 `load_package.txt` 中列出的扩展模块
3. 读取 pkl 数据文件（`Tasker_list.pkl`）
4. 进入命令循环，显示 `[command]main>` 提示符

**退出程序：** 输入 `exit`

## 二、主界面指令（Method 层）

以下指令在主命令提示符 `main>` 下输入。指令与参数用空格分隔。

### 2.1 默认模块 (default_pkg)

#### Tasker 管理

| 指令 | 参数 | 功能 | 示例 |
|------|------|------|------|
| `get` | Tasker索引/标签 | 进入指定 Tasker 内部界面 | `get 0` 或 `get 班级` |
| `add` | Tasker模板索引/版本 | 创建新 Tasker（容器） | `add` → 选择模板 |
| `delete` | Tasker索引/标签 | 删除 Tasker 及其所有 Task | `delete 班级` |
| `edit` | Tasker索引/标签 | 编辑 Tasker 的标签和描述 | `edit 0` |
| `info` | Tasker索引/标签 | 查看 Tasker 详细信息 | `info 班级` |

#### 全局操作

| 指令 | 参数 | 功能 | 示例 |
|------|------|------|------|
| `backup` | 无 | 备份所有数据到 `./backup_all/` | `backup` |
| `reload` | `u`(自动升级) / `n`(不升级) | 从备份文件恢复数据 | `reload u` |
| `txt` | `1`(按日期) / `2`(按Tasker) / `3`(按创建日期) | 生成可读 txt 文件 | `txt 2` |

#### 系统指令

| 指令 | 参数 | 功能 | 示例 |
|------|------|------|------|
| `sys_info` | 系统变量名 | 查看 system_pkg 中指定变量 | `sys_info version` |
| `save_pkl` | 无 | 手动保存 pkl 文件 | `save_pkl` |

#### 排列操作

| 指令 | 参数 | 功能 | 示例 |
|------|------|------|------|
| `sort` | 排列字段(create_date/tasker_label/type/version) | 按字段排序 Tasker 列表 | `sort create_date` |
| `top` | Tasker索引/标签 | 将指定 Tasker 置顶 | `top 班级` |
| `insert` | Tasker索引/标签 → 目标索引 | 将 Tasker 移动到指定位置 | `insert 班级` → `0` |

#### 默认 Method 机制

如果 `settings.txt` 中设置了 `DEFAULT_METHOD`（如 `DEFAULT_METHOD|get`），则在 main 下直接输入参数即可自动调用该 Method。例如设置了 `DEFAULT_METHOD|get`，输入 `班级` 等同于 `get 班级`。

### 2.2 班级事务模块 (school_task)

| 指令 | 参数 | 功能 | 说明 |
|------|------|------|------|
| `today` | 日期(可选，默认今天) | 生成今日待办文件 `today.txt` | 筛选所有 Tasker 中 date 匹配的 task |
| `txt1` | 无 | 生成简洁版汇总文件 `task1.txt` | 按 Tasker 分组列出所有 task |
| `backup1` | 无 | 生成早期格式备份 `backup1.txt` | 格式：`Task_object|标签|日期|描述` |
| `reload1` | 无 | 从 `backup1.txt` 恢复数据 | 仅支持 Default 类型，会丢失 Extra 数据 |

### 2.3 Tasker 功能管理模块 (tasker_manager)

| 指令 | 参数 | 功能 | 示例 |
|------|------|------|------|
| `add_func` | Tasker索引/标签 | 为指定 Tasker 添加新的 class_func | `add_func 0` → 选择功能 |
| `del_func` | Tasker索引/标签 | 从 Tasker 移除指定 class_func | `del_func 班级` → 选择要删除的功能 |

## 三、Tasker 内部界面指令（class_func 层）

进入 Tasker 后，提示符变为 `{tasker_label}>`。界面默认显示最后 8 条 Task 记录。

**界面特殊输入：**
- `/` 开头 → 强制识别为指令（如 `/new`）
- `+` 开头 → 强制识别为搜索参数（如 `+回执`）
- `/info` → 显示当前可用指令列表
- 直接输入索引号 → 搜索该索引的 Task 详情
- 直接输入字符串 → 执行搜索
- `exit` → 返回主界面
- 空输入 → 刷新界面

### 3.1 默认功能 (default_tasker_func)

所有 Default 类型 Tasker 默认具备以下功能：

| 指令 | 参数 | 功能 |
|------|------|------|
| `search` | 索引 或 搜索关键词 | 搜索 Task。数字按索引精确查找，字符串在 content/date/attribute/comment 中搜索 |
| `new` | `n`(创建空Task) 或 无参数 | 创建新 Task。依次输入日期（默认今天）、属性、内容、注释 |
| `delete` | 索引或搜索词 | 删除 Task。默认选中搜索结果的最后一项（列表末端） |
| `edit` | 索引或搜索词 | 编辑 Task。依次展示当前值并询问新值（回车保留原值） |
| `backup` | `e`(含Extra类型) | 备份当前 Tasker 到 `./backup_single/` |
| `reload` | 无 | 从 `./backup_single/` 恢复当前 Tasker 数据 |

**Task 字段说明：**
- **日期 (date)**：事件日期，格式 `YYYY_MM_DD`
- **属性 (attribute)**：分类标签，如"回执"、"购买"、"高考"、"操作"、"通知"
- **内容 (content)**：自由文本。早期约定用 `"; "`（分号+空格）分隔多项信息，便于未来解析
- **注释 (comment)**：补充信息，可选

### 3.2 统计分析功能 (categorize)

需通过 `add_func` 添加 `categorize_task` 到 Tasker 后使用。

| 指令 | 参数 | 功能 |
|------|------|------|
| `categorize` | `attr` 或 `date` | 统计分析并生成图表（饼图+柱状图） |

- `attr`：按 attribute 字段统计各分类数量
- `date`：按月份统计 Task 数量变化趋势

### 3.3 账号管理功能 (account)

需创建 account 类型 Tasker 使用。account 类型 Tasker 的 Task 字段不同：

| 字段 | 说明 |
|------|------|
| 账号类型 (account_type) | 所属平台（如"GitHub"、"Google"） |
| 标签 (label) | 搜索用标签，默认同 account_type |
| 密码 (password) | 存储的密码 |

**补充信息 (dict)：** 支持 description、login_name、verified_phone、verified_email、linked_account、secure_question、other_info、password_history 等类型，格式为 `类型|||内容1|||内容2`。

| 指令 | 功能 |
|------|------|
| `new` | 创建新账号记录。输入账号类型、标签，可选补充各类信息 |
| `get` | 搜索账号。按标签 > 索引 > 账号类型 > 关联账号的优先级搜索，唯一匹配时自动复制密码到剪贴板 |
| `search` | 查看账号详细信息（含所有补充信息） |
| `edit` | 编辑账号的补充信息 |
| `delete` | 删除账号记录 |
| `update` | 更新密码（旧密码自动迁移到 password_history） |

**管理指令 (account_manage_func)：**

| 指令 | 功能 |
|------|------|
| `list_acc` | 列出当前 Tasker 中所有账号的概要信息 |

### 3.4 计时器功能 (timer)

需创建 timer 类型 Tasker 使用。timer 类型 Task 字段：

| 字段 | 说明 |
|------|------|
| start_time | 开始时间，格式 `YYYY_MM_DD-HH:MM` |
| end_time | 结束时间，格式 `YYYY_MM_DD-HH:MM`（空表示进行中） |
| attribute | 属性/分类 |
| content | 内容描述 |
| comment | 注释 |

**可配置默认值：** 通过 `config` 指令设置默认属性、内容前缀、默认内容。

| 指令 | 功能 |
|------|------|
| `new` | 创建新计时记录。依次输入开始时间、属性、内容、注释、结束时间（可留空） |
| `edit` | 编辑计时记录。按时间/属性/内容/注释/索引搜索定位后修改 |
| `config` | 配置默认值：默认属性、内容前缀、默认内容 |
| `delete` | 删除计时记录 |
| `search` | 搜索计时记录（支持按时间范围、属性、内容、注释、索引搜索） |
| `end` | 结束一个未完成的计时（自动填充当前时间为 end_time） |

**分析功能 (timer_analyze_func，需手动添加)：**

| 指令 | 参数 | 功能 |
|------|------|------|
| `distribution` | `all` / `attr` / `date` | 时间分布箱型图（框架已定义，功能待实现） |
| `count` | `attr` / `date` | 按属性或月份统计数量（框架已定义） |
| `trend` | `all` / `attr` | 时间趋势点图（框架已定义） |
| `duration` | `all` / `attr` / `date` | 时长统计（框架已定义） |

**分类功能 (timer_categorize_func)：**

| 指令 | 参数 | 功能 |
|------|------|------|
| `categorize` | `attr` 或 `date` | 按时长和天数分类统计（attr 已实现饼图+柱状图+表格） |

### 3.5 标签功能 (label)

需创建 label 类型 Tasker 使用。为 Task 添加自定义键值对标签，支持按标签搜索。

## 四、数据备份与恢复

### 备份方式

| 方式 | 指令 | 输出位置 | 格式 |
|------|------|------|------|
| 自动 pkl 备份 | 自动 | `./backup_pkl/` | Python pickle（协议5） |
| 全局 txt 备份 | `backup`(main) | `./backup_all/` | `||||`分隔的 txt |
| 单 Tasker 备份 | `backup`(Tasker内) | `./backup_single/` | 同上格式 |
| 可读 txt | `txt`(main) | `./Task_txt.txt` | 树形/按日期排序 |

### 恢复方式

| 方式 | 指令 | 说明 |
|------|------|------|
| 全局恢复 | `reload`(main) | 从 `./backup_all/` 选择备份文件导入 |
| 单 Tasker 恢复 | `reload`(Tasker内) | 从 `./backup_single/` 恢复当前 Tasker |

## 五、配置说明

### settings.txt

每行格式：`配置项|值`

| 配置项 | 默认值 | 说明 |
|------|------|------|
| `SHOW_TIPS` | True | 是否显示绿色提示信息 |
| `AUTO_SAVE` | True | 每次命令后自动保存 pkl |
| `AUTO_BACKUP` | True | 自动备份 pkl |
| `BACKUP_INTERVAL` | 3 | pkl 备份最小间隔（天） |
| `BACKUP_TOTAL` | 3 | 保留的最大 pkl 备份数 |
| `DEFAULT_METHOD` | (空) | 默认执行的 Method（如 `get`） |

### load_package.txt

每行一个模块名，按需加载。当前配置：

```
categorize
tasker_manager
school_task
account
timer
label
custom_package
```

## 六、记录数据的约定格式

早期由于技术水平限制，通过约定字符串内部格式来实现结构化数据：

**content 字段内部约定：**
- 多项信息用 `"; "`（分号+空格）分隔
- 文件引用：`"file:.\AI\2023_09_28班级.txt"`
- 图片引用：`"jpg:少儿住院互助基金告家长书"`
- 文档引用：`"docx:综评网基础成绩核对通知"`
- PDF 引用：`"pdf:附件三：高中生研究性学习课题认证学生操作指导手册"`
- 键值对：`"卡号:******密码:******"`、`"缴费单号0749******"`
- 金额记录：`"15元*20天"`
- 链接：`"http://********.********.****.edu.cn/"`

这些约定使得纯文本记录具备了一定的可解析性，为未来用新脚本批量转换和分析提供了基础。

## 七、日志系统

- `./log/log_YYYY_MM_DD.txt`：每次命令执行记录（时间、指令、结果）
- `./error_log/exception_YYYY_MM_DD - HH-MM-SS.txt`：异常堆栈信息
