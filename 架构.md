# Task 项目架构文档

> 本文档基于 2023-2024 年原始代码分析撰写，旨在帮助快速理解早期设计意图与代码组织方式。

## 一、项目概览

Task 是一个基于 Python 命令行的个人日常记录系统，采用**模板-实例**分离的插件化架构。核心理念是：数据以 **Tasker（容器）→ Task（记录项）** 的层级组织，功能通过可插拔的模块（package）扩展，模块间通过统一接口通信。

项目起始于高中阶段（约 2023 年 9 月），最初用于记录班级事务（作业、通知、回执等），后来逐步扩展了计时、账号管理、标签分类等能力。

## 二、分层架构

```
┌─────────────────────────────────────────┐
│              main.py (入口)              │
│  命令解析 → Method 路由 → 执行/日志/保存  │
└──────────────┬──────────────────────────┘
               │ 调度
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌────────┐ ┌──────┐ ┌──────────┐
│ Method │ │  IO  │ │ Message  │
│ (指令) │ │(输入)│ │ (输出)   │
└───┬────┘ └──────┘ └──────────┘
    │ 调用
    ▼
┌─────────────────────┐
│    Tasker (容器)     │  ← 持有 task_list, function_list
│  interface / update  │
└─────────┬───────────┘
          │ 包含
    ┌─────┴─────┐
    ▼           ▼
┌────────┐ ┌──────────────┐
│  Task  │ │ class_func   │  ← Tasker 内置功能（search/new/delete 等）
│(记录项)│ │ (功能类)      │
└────────┘ └──────────────┘
```

### 2.1 核心层级

| 层级 | 文件 | 职责 |
|------|------|------|
| **入口层** | `main.py` + `function.py` | 程序启动、模块加载、命令循环、pkl持久化、日志 |
| **指令路由层** | `default_method.py` → 各模块 `method.py` | 解析用户输入的命令，匹配 Method，执行 proceed |
| **容器层** | `default_class.py` → 各模块 `tasker.py` | Tasker 作为数据容器，持有 task_list 和 function_list |
| **记录层** | `default_class.py` → 各模块 `task.py` | Task 为单条记录，包含 date/attribute/content/comment |
| **功能层** | `default_class_func.py` → 各模块 `class_func.py` | 挂载在 Tasker 上的功能（在 interface 界面内调用） |
| **IO/消息层** | 各模块 `io.py` / `message.py` | 可替换的输入输出样式（ANSI 颜色、表格渲染） |

### 2.2 模板继承体系

项目核心设计模式：**基类定义协议 → 模板提供默认实现 → 具体模块继承扩展**。

```
default_class.py (基类，定义接口)
    ├── default_tasker_template  ──→  package/template/tasker.py (模板)
    ├── default_task_template    ──→  package/template/task.py
    ├── extra_tasker_template    ──→  package/template/tasker.py
    └── extra_task_template      ──→  package/template/task.py

default_class_func.py (基类)
    ├── default_tasker_func_template → package/template/class_func.py
    └── extra_tasker_func_template   → package/template/class_func.py

default_method.py (基类)
    └── default_method_template  → package/template/method.py
```

每个具体模块继承 template 或直接继承基类，形成两层或三层继承链。例如：

```
default_task_template → df_task_template(template) → default_task(default_pkg)
```

### 2.3 两种类型系统

系统区分 **Default** 和 **Extra** 两种类型：

| 特性 | Default | Extra |
|------|---------|-------|
| 版本比较 | 数值比较（float） | 字符串精确匹配 |
| 兼容策略 | 高版本兼容低版本（≤） | 仅同版本兼容 |
| 典型模块 | `default_pkg` | `account`, `timer`, `label` |
| 数据格式 | 固定字段（date, attribute, content, comment） | 自定义字段 |

这一设计的意图是：Default 提供稳定、向后兼容的基础功能；Extra 允许各模块自由定义数据结构，不受 Default 字段限制。

## 三、模块（Package）系统

### 3.1 模块加载机制

模块位于 `./package/` 目录，每个模块是一个 Python 包，必须包含 `__init__.py` 并导出 `package_dict`。`main.py` 在启动时：

1. 强制加载 `default_pkg`（基础功能）
2. 读取 `load_package.txt`，按行加载其余模块
3. 每个模块的 `package_dict` 贡献：class_func、default_tasker、extra_tasker、io、message、method、default_task、extra_task

### 3.2 现有模块一览

| 模块 | 类型 | 功能 |
|------|------|------|
| `default_pkg` | Default | 核心 CRUD：get/add/delete/edit/info Tasker；search/new/delete/backup/edit/reload Task；backup/reload/txt 全局备份；sort/top/insert 排列；sys_info/save_pkl 系统指令 |
| `template` | 模板 | 供开发者参考的模板代码，不实际加载 |
| `account` | Extra | 账号密码管理（含剪贴板复制密码、关联账号搜索） |
| `timer` | Extra | 时间记录（记录开始/结束时间，统计时长、属性分类、图表分析） |
| `label` | Extra | 标签系统（为 Task 添加自定义标签对，支持按标签搜索） |
| `categorize` | Default | 统计分析（按属性/日期生成饼图+柱状图） |
| `school_task` | Default | 早期班级事务专用：today/txt1/backup1/reload1 |
| `tasker_manager` | Default | Tasker 功能管理：add_func/del_func（动态增减 Tasker 的 class_func） |
| `custom_package` | 自定义 | 用户自定义模块模板 |

### 3.3 模块间的通信

模块通过 `system_pkg` 字典通信。`system_pkg()` 函数（在 `main.py` 中定义）将以下内容打包为字典，传递给所有 Method 和 class_func：

- 消息函数：`system_msg`, `error_msg`, `tips_msg`, `table_msg`, `head_msg`, `body_msg`, `normal_msg`
- 输入函数：`command_input`, `normal_input`, `strict_input`, `block_input`
- 常量：`CONDITION_SUCCESS`, `CONDITION_FAIL`, `BLOCK_LIST`, `EXIT`
- 类型常量：`TYPE_DEFAULT_TASKER` 等
- 模板列表：`df_tasker_template_list`, `class_func_list` 等
- 运行时配置：`settings_dict`, `main_pkl_dir`, `save_pkl`

## 四、核心数据结构

### 4.1 Tasker（容器）

```python
class default_tasker_template:
    tasker_label: str      # 容器名称（如"班级"）
    description: str       # 描述
    function_list: list    # 挂载的 class_func 实例列表
    create_date: str       # 创建日期 (YYYY_MM_DD)
    task_list: list        # 包含的 Task 实例列表
    task_template: list    # 可用的 Task 模板类列表
    version: str           # 版本号
    type: str              # "Default" 或 "Extra"
```

### 4.2 Task（记录项）

Default 类型 Task 字段：
```python
class default_task_template:
    create_date: str    # 记录创建日期
    date: str           # 事件日期
    attribute: str      # 属性/分类（如"回执"、"购买"、"高考"）
    content: str        # 内容（自由文本，内部用"; "分隔多段信息）
    comment: str        # 注释
```

Extra 类型 Task 可自定义字段，如 `timer_task` 包含 `start_time`, `end_time`；`account_task` 包含 `account_type`, `label`, `password`, `dict` 等。

### 4.3 Method（指令）

```python
class default_method_template:
    label: str          # 方法标签
    version: str        # 版本
    type: str           # "Default" / "Extra"
    method_list: list   # 支持的指令列表（如 ["get","add","delete"]）
    analyze()           # 分析用户输入是否匹配本 Method
    proceed()           # 执行匹配的指令
```

### 4.4 class_func（容器内功能）

```python
class default_tasker_func_template:
    label: str          # 功能标签
    function_list: list # 支持的功能列表（如 ["search","new","delete"]）
    proceed()           # 通过 getattr 动态调用对应方法
```

## 五、数据流

### 5.1 主循环流程

```
用户输入 → command_input("main")
    → get_cmd_list() 解析为 [cmd, parameter]
    → search_method() 遍历所有 method_list，调用 analyze()
    → 匹配成功 → method.proceed() 执行
    → write_log() 记录日志
    → auto_save_pkl() 自动持久化
    → 循环
```

### 5.2 Tasker 内部交互流程

```
method.get("班级", tasker_list) → 找到 Tasker
    → tasker.interface(system_pkg)
        → 显示最后 8 条 Task
        → 用户输入 "/new" 或 "new"
        → 匹配 class_func.function_list
        → class_func.proceed(["new", "..."], tasker, system_pkg)
            → 创建 Task 实例 → 追加到 tasker.task_list
```

## 六、持久化机制

### 6.1 pkl 文件（主存储）
- 使用 Python `pickle` 协议 5 存储整个 `main_tasker_list`
- 默认路径：`./Tasker_list.pkl`
- 自动保存：每次命令执行后（可通过 `settings.txt` 关闭）
- 自动备份：按日期间隔生成 `./backup_pkl/backup_YYYY_MM_DD - HH-MM-SS.pkl`

### 6.2 txt 备份
- 全局备份：`./backup_all/backup_YYYY_MM_DD - HH-MM-SS.txt`
- 可读 txt：`./Task_txt.txt`（支持按日期/按Tasker/按创建日期三种排序）

### 6.3 日志
- 普通日志：`./log/log_YYYY_MM_DD.txt`
- 错误日志：`./error_log/exception_YYYY_MM_DD - HH-MM-SS.txt`

## 七、配置系统

- `settings.txt`：键值对配置（`SHOW_TIPS`, `AUTO_SAVE`, `AUTO_BACKUP`, `BACKUP_INTERVAL`, `BACKUP_TOTAL`, `DEFAULT_METHOD`）
- `load_package.txt`：按行列出要加载的模块名
- `pkl_dir.txt`：自定义 pkl 存储路径

## 八、设计特点与局限

**亮点：**
- 模板-实例分离的插件架构在当时（初学者阶段）展现了良好的模块化思维
- `system_pkg` 字典式的依赖注入避免了模块间硬耦合
- 版本兼容机制（Default 数值比较、Extra 精确匹配）
- ANSI 彩色终端输出和表格渲染

**局限：**
- 无可视化 GUI，纯命令行交互
- pkl 格式不具备可读性和跨版本兼容性
- 大量全局变量（`main.py` 中十余个 `global`）
- Python 3.12 的 f-string 引号嵌套语法 (`f"{str(e).split("'")[1]}"`) 不够健壮
- `default_class.py` 长达 350+ 行，承担了过多职责
- 缺少单元测试
